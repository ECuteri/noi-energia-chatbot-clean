# Modulo Agents

Questo modulo è stato deprecato. Gli agenti chatbot ora si trovano nella directory `chatbots/`.

Vedi la directory `chatbots/` per l'implementazione corrente del chatbot.

### Agent Factory
Il `chatbots/common/agent_factory.py` fornisce una semplice funzione factory per creare agenti chatbot con configurazione consistente.

### Tool
Tutti i tool chatbot ora sono in `chatbots/common/tools/`:
- `list_documents.py`
- `get_file_contents.py`
- `vector_search.py`

## Documentazione

Per documentazione corrente, vedi:
- README Principale: `../README.md`
- Chatbots: `../chatbots/`
- Database: `../database/README.md`
